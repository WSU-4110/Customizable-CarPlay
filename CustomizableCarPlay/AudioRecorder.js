// AudioRecorder.js
import { Audio } from 'expo-av';

export const recordAudio = async () => {
  // ... audio recording logic
};

export const sendAudioForTranscription = async (audioUri) => {
  // ... send audio for transcription
};
